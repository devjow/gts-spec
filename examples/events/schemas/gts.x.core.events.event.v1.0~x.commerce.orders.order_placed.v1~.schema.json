{
  "$id": "gts.x.core.events.event.v1.0~x.commerce.orders.order_placed.v1.0~",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Event Instance Schema: order.placed",
  "type": "object",
  "allOf": [
    { "$ref": "gts.x.core.events.event.v1.0~.schema.json" },
    {
      "type": "object",
      "required": ["gtsId", "payload", "subjectType"],
      "properties": {
        "gtsId": {
          "const": "gts.x.core.events.event.v1.0~x.commerce.orders.order_placed.v1.0~",
          "$comment": "The value is required only for IDE-side validation"
        },
        "payload": {
          "type": "object",
          "required": ["orderId", "customerId", "totalAmount", "items"],
          "properties": {
            "orderId": { "type": "string", "format": "uuid" },
            "customerId": { "type": "string", "format": "uuid" },
            "totalAmount": { "type": "number" },
            "items": { "type": "array", "items": { "type": "object" } }
          }
        },
        "subjectType": { "pattern": "^gts.x.commerce.orders.order.v1.0~*$" }
      }
    }
  ]
}
